name: Build macOS

# Phase 3 (see PLAN.md) — not active in V1. Enable by adding a tag/dispatch
# trigger once macOS packaging (codesigning/notarization) has been set up.
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "macOS build pipeline is planned for Phase 3 and not yet implemented."
